# macOS metadata
.DS_Store

# Local generation scripts, renders, caches, and exported planning/UI artifacts.
# Markdown requirements under docs/ are the versioned planning source.
/tmp/
/output/

# Local secrets; commit sanitized examples when configuration is introduced.
.env
.env.*
!.env.example
!.env.*.example

# Installed dependencies, build products, and generated native projects.
node_modules/
dist/
dist-test/
coverage/
.expo/
/apps/mobile/ios/
/apps/mobile/android/
*.tsbuildinfo
*.log
/data/

# Firebase service-account private credentials must never enter Git.
*firebase-adminsdk*.json

# Supply Android Firebase client configuration locally or through an EAS file variable.
/apps/mobile/google-services.json
